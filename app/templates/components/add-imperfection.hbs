{{#if addNewImperfection}}
  <h3>Give me all your imperfections!</h3>
  <div>
    <form>
      <div class="form-group">
        <label for="author">auTHor</label>
        {{input value=author id="author"}}
      </div>
      <div class="form-group">
        <label for="category">cAtegorY</label>
        {{input value=category id="category"}}
      </div>
      <div class="form-group">
        <label for="image">imagE</label>
        {{input value=image id="image"}}
      </div>
      <div class="form-group">
        <label for="location">LocaShiOn</label>
        {{input value=location id="location"}}
      </div>
      <div class="form-group">
        <label for="latitude">latitudE</label>
        {{input value=latitude id="latitude"}}
      </div>
      <div class="form-group">
        <label for="longitude">longiTude</label>
        {{input value=longitude id="longitude"}}
      </div>
      <div class="form-group">
        <label for="title">tYtle</label>
        {{input value=title id="title"}}
      </div>
      <button {{action "saveImperfection"}} name="saveImperfection">Save my imperfection</button>
    </form>
  </div>
{{else}}
  <button class="btn btn-sm" {{action 'showNewImperfectionForm'}}>add imperfection</button>
{{/if}}
