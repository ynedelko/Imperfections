<ul class='no-bull'>
  <li>{{imperfection.title}} at {{imperfection.location}}:
  <p><img class="img-round" src="{{imperfection.image}}" alt="{{imperfection.title}}" /></p></li>
  <button class="btn btn-xs btn-success" {{action 'delete' imperfection}}>too perfect...delete!</button>
</ul>
